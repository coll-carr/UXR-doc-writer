openapi: 3.1.0
info:
  title: Google Docs API for Writing Agentic Output
  description: Create and update Google Docs with content generated by an agentic workflow.
  version: 1.0.0

servers:
  - url: https://docs.googleapis.com/v1

paths:
  /documents:
    post:
      operationId: createDocument
      summary: Create a new Google Doc
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
              required:
                - title
      responses:
        '200':
          description: Document created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  documentId:
                    type: string
                  title:
                    type: string

  /documents/{documentId}:batchUpdate:
    post:
      operationId: writeToDocument
      summary: Write content to a Google Doc
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                requests:
                  type: array
                  items:
                    type: object
              required:
                - requests
      responses:
        '200':
          description: Content inserted into the document
